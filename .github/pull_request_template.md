## 관련 이슈

Closes #

<!-- 머지되면 이슈가 자동으로 닫히고 보드 카드도 Done 으로 갑니다.
     단, default 브랜치를 대상으로 하는 PR 에서만 동작합니다. -->

## 접근 방법

<!-- 이 칸은 직접 쓰세요. AI 에게 채우게 하면 이 PR 의 의미가 없어집니다.

     예) 선택한 방식        — 기존 목록 컴포넌트를 재사용하고 필터만 추가
         고른 이유          — 새로 만들면 정렬·페이징을 다시 구현해야 함
         선택하지 않은 방식  — 전용 화면 신설. 유지보수 지점이 둘로 갈라져서 접음 -->

- 선택한 방식:
- 고른 이유:
- 고려했지만 선택하지 않은 방식:

## 변경 범위와 영향

<!-- 예) 변경하지 않은 영역   — 저장 로직은 안 건드림
         영향받을 수 있는     — 같은 컴포넌트를 쓰는 다른 화면 -->

- 변경한 영역:
- 변경하지 않은 영역:
- 영향받을 수 있는 모듈·기능:
- 더 쪼갤 수 없는 이유:  (PR 이 클 때만)

## 검증

<!-- 재현 절차는 QA 하는 사람이 이것만 보고 그대로 밟습니다. 떨어져서 일하면 물어볼 수 없습니다.
     "확인했습니다" 가 아니라 "이렇게 하면 이게 보입니다" 로 적으세요.

     화면:   어느 메뉴 → 어느 화면
     데이터: 특정 데이터가 필요하면 그 이름이나 조건 -->

- 자동 테스트:
- 재현 절차:
  1.
  2.
  3.
- 확인이 필요한 실패·경계 조건:

## 리뷰어가 특히 봐야 할 곳

<!-- 확신이 덜한 부분 · AI 결과를 그대로 둔 부분 · 판단이 갈릴 수 있는 부분.

     비워두지 마세요. "없음" 도 답이고, 그러면 리뷰어는 "정말 없나" 를 먼저 묻습니다.

     예) 정렬 조건이 여러 개일 때 처리 — 한 가지 경우만 확인했습니다 -->

-

## 체크리스트

- [ ] `접근 방법` 과 `리뷰어가 특히 봐야 할 곳` 을 내가 직접 썼다
- [ ] 로컬 동작 확인
- [ ] 테스트 추가 또는 수정
